import { useAuth } from '../context/AuthContext'
import { Card, Field } from '../components/UI'
export default function Profile(){const {user,docente}=useAuth();return <><div className="page-intro"><div><p>Cuenta</p><h2>Mi perfil</h2><span>Información de la cuenta docente autenticada.</span></div></div><Card className="profile-card"><div className="profile-avatar">{(docente?.nombre||user?.email||'U').charAt(0).toUpperCase()}</div><div className="profile-grid"><Field label="Nombre"><input readOnly value={docente?.nombre||user?.user_metadata?.nombre||''}/></Field><Field label="Apellido"><input readOnly value={docente?.apellido||user?.user_metadata?.apellido||''}/></Field><Field label="Correo"><input readOnly value={docente?.correo||user?.email||''}/></Field><Field label="ID de usuario"><input readOnly value={user?.id||''}/></Field></div></Card></>}
